export const ADMIN_OFFICES_PAGE_SIZE = 20;

/** Khi xem phân cấp — gom theo ủy ban/tỉnh (tối đa một lần tải). */
export const ADMIN_OFFICES_HIERARCHY_PAGE_SIZE = 500;

export type AdminOfficesViewMode = 'hierarchy' | 'table';
