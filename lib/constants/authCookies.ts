/** Cookie names for auth (HttpOnly access/refresh set by `/api/auth/*`; must match proxy). */
export const AUTH_COOKIE_ACCESS = 'gl_access_token';
export const AUTH_COOKIE_REFRESH = 'gl_refresh_token';
export const AUTH_COOKIE_MUST_CHANGE_PASSWORD = 'gl_must_change_password';
