/** RHF — validate ngay khi gõ (admin / company forms). */
export const REALTIME_FORM_OPTIONS = {
  mode: 'onChange' as const,
  reValidateMode: 'onChange' as const,
};

/** Ô tìm kiếm / lọc — chỉ giới hạn độ dài, không bắt tối thiểu. */
export const SEARCH_INPUT_MAX_LENGTH = 200;
