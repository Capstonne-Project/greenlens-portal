import { UserProfileView } from '@/components/profile';

/** @deprecated Import `UserProfileView` from `@/components/profile` with `variant="admin"`. */
export function AdminProfilePage() {
  return <UserProfileView variant="admin" />;
}
