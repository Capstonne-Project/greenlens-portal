/** Chiều cao khung map so sánh — co theo viewport, không kéo MapLibre khi sync skeleton. */
export const COMPARE_MAP_FRAME_HEIGHT =
  'h-[min(48vw,14rem)] min-h-48 w-full sm:h-[min(38vw,18rem)] sm:min-h-56 md:h-[min(34vw,22rem)] md:min-h-64 lg:h-[min(32vw,26rem)] lg:min-h-72';
