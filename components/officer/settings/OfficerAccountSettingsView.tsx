import { UserProfileView } from '@/components/profile';

/** @deprecated Import `UserProfileView` from `@/components/profile` with `variant="officer"`. */
export function OfficerAccountSettingsView() {
  return <UserProfileView variant="officer" />;
}
