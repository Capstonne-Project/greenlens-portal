import { UserProfileView } from '@/components/profile';

/** @deprecated Import `UserProfileView` from `@/components/profile` with `variant="company"`. */
export function CompanyAccountView() {
  return <UserProfileView variant="company" />;
}
