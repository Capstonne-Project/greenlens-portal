# SU26SE049 — cấu trúc & overview

**Tổng quan dự án (không tham chiếu dự án mẫu ngoài capstone):**

- **`docs/Overview.md`** — bản chuẩn: phạm vi, tech stack, cây thư mục, lớp API, route, BR, performance, security, cấu hình lần đầu, **Phụ lục `globals.css`**.

File `docs/PROJECT_HANDBOOK_SU26SE049.md` (nếu còn) là bản trung gian; khi cần cập nhật Overview, có thể chỉnh trong `Overview.md` trực tiếp hoặc chạy `node scripts/generate-overview-md.mjs` sau khi sửa handbook (nguồn gốc).
