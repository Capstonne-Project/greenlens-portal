# Android APK (optional local fallback)

Landing **Tải ứng dụng** mặc định trỏ Expo build:

https://expo.dev/accounts/phuchau23/projects/green-lens-app/builds/547c929a-8c6d-4d5a-b864-d5f0b0a8611c

Ghi đè bằng `NEXT_PUBLIC_ANDROID_APK_URL` trong `.env.local`, hoặc đặt file tại `greenlens.apk` và set env tới `/downloads/greenlens.apk`.
